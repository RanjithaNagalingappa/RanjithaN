s_num= int(input("Enter the starting range: "))
e_num= int(input("Enter the end range: "))+1
for n in range(s_num, e_num ):
    if n > 1:
        for i in range(2, n):
            if n % i == 0:
                break
        else:
            print(n)
